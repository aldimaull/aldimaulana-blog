---
const { text, link, className, btnClass, count } = Astro.props;
---

<button
 class=`p-1 rounded-full bg-tertiary-container-light text-on-tertiary-container-light dark:bg-tertiary-container-dark dark:text-on-tertiary-container-dark ${btnClass}`
 ><a href=`${link}` class=`${className}`>{text}</a></button
>
